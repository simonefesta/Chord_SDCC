sudo groupadd docker
sudo usermod -aG docker ${USER}
docker run hello-world (usando dockerfile proposto dalla guida di aws)

sudo chmod 666 /var/run/docker.sock nel docker 

sudo yum install golang -y (installa go)
https://medium.com/@fredmanre/how-to-configure-docker-docker-compose-in-aws-ec2-amazon-linux-2023-ami-ab4d10b2bcdc (docker compose)
funziona come sempre
ssh -i ~/.ssh/AWSKeypair.pem ec2-user@34.201.121.188 (from any folder)

exit per chiudere

