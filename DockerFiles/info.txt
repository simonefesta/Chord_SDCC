0) aprire docker
1) python generate_compose.py <numero containers>

2) da [Chord_SDCC]:
	docker-compose build
	docker-compose up
3) da [Chord_SDCC/client]
	go build
	./client
4) per chiudere tutto:
	docker stop $(docker ps -q)
	docker-compose down

